#include <stdio.h>
/**
 * main- writte the alphabet
 * return in lowercase
 *
 * Return:0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
